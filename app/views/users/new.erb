<%= form_for @user do |f|%>
    <%= f.label :name%>
    <%= f.text_field :name%>
    <%= f.label :bio%>
    <%= f.text_area :bio%>
    <%= f.collection_select :area_id, Area.all, :id, :name %>
    <%= f.submit%>
    <%end %>
